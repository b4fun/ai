{
  "name": "@b4fun/ai-cli",
  "version": "0.1.0-alpha.8",
  "description": "Minimal CLI wrapper around pi coding agent.",
  "type": "module",
  "bin": {
    "ai": "bin/ai.js"
  },
  "files": [
    "bin",
    "src",
    "scripts",
    "README.md",
    "install.sh"
  ],
  "scripts": {
    "start": "node bin/ai.js",
    "check": "node --check src/index.js && node --check src/auth.js && node --check src/config.js && node --check src/model-config.js && node --check src/profiles.js && node --check src/version.js && node --check bin/ai.js && node --check scripts/build-sea.mjs",
    "test": "node --test",
    "build:sea": "node scripts/build-sea.mjs",
    "build": "npm run build:sea"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "^0.80.2",
    "typebox": "1.1.38"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.28.1",
    "postject": "^1.0.0-alpha.6"
  }
}
